[project]
name = "langgraph-sample-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "deepagents",
    "langchain-openai",
    "python-dotenv",
    "cryptography>=44.0.0",
    "httpx",
    "tavily-python",
]
